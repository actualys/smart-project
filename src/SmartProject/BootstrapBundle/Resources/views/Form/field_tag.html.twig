{% block tag_widget %}
    {% spaceless %}
        {% set attr = attr|merge({'class': attr.class|default('') ~ ' form-field-tag'}) %}
        {{ block('form_widget_simple') }}
    {% endspaceless %}
{% endblock tag_widget %}