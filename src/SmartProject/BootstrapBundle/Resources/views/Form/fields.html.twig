{% block tag_widget %}
    {% spaceless %}
        {% set attr = attr|merge({'class': attr.class|default('') ~ ' select2 tags form-control'}) %}
        {{ block('form_widget_simple') }}
    {% endspaceless %}
{% endblock tag_widget %}