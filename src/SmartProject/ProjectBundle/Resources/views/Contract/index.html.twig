{% extends 'SmartProjectFrontBundle::layout.html.twig' %}

{% block headline %}
    Contract list
{% endblock %}

{% block content_content %}
    <table class="table table-condensed table-striped table-hover">
        <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Status</th>
            <th>Default</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('contract_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.status }}</td>
                <td>{{ entity.default }}</td>
                <td>
                    <a href="{{ path('contract_show', { 'id': entity.id }) }}" class="btn btn-default btn-xs">show</a>
                    <a href="{{ path('contract_edit', { 'id': entity.id }) }}" class="btn btn-default btn-xs">edit</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}

{% block content_sidebar %}
    <a href="{{ path('contract_new') }}" class="btn btn-primary btn-block">Create a new contract</a>
{% endblock %}